# DvUi

## Base Blade Components

Interface for using components

### Setup

To configure the environment so that PhpStorm recognizes the views correctly, execute the following command:

```bash
cd /ModDev/DvUi
ln -s resources Resources
